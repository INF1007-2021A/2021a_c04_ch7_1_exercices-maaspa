#!/usr/bin/env python
# -*- coding: utf-8 -*-

# TODO: Importez vos modules ici

import random

def arbre():
    pendown
    forward(x)
    right(20)
    forward(x/2)
    right(180)
    forward(x/2)
    right(160)
    
    
    
# TODO: Définissez vos fonction ici
def ellipsoide(A,B,C,masse_volumique):
    volume = (4/3 * π * A * B * C)
    masse = masse_volumique * volume
    return volume, masse

def adn(chaine):
    assert chaine != []
    
    for lettre in chaine:
        if lettre != ("a" or "t" or "g" or "c"):
            return False
        else:
            return True
        
def saisie():
    lettres = ("a","t","g","c")
    for i in range (randrange(10):
        for j in range (len(lettres):
            sequence_adn += lettres[j] * randrange(10)
    return sequence_adn
if __name__ == '__main__':
    # TODO: Appelez vos fonctions ici

    pass
